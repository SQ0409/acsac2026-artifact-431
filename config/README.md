# Configuration

`ngap_nas.dict` is the protocol dictionary. `knowledge.json` is a deliberately small public example showing selector matching and enum mutation; the private rule set is not included. Copy `config.example.env` to `local.env` and replace the endpoint with the reviewer's SCTP address. 
