{
  "version": "1.0",
  "description": "Public one-rule example for the knowledge-guided NGAP mutator.",
  "rules": [
    {
      "id": "R-NGSETUP-ADJACENT",
      "description": "For an NGSetupRequest, select one adjacent procedure code.",
      "selector": { "any_of": [ { "ngap.procedure_code": [21] } ] },
      "actions": [
        { "loc": "ngap.procedure_code", "op": "enum_set", "values": [20, 22] }
      ]
    }
  ]
}
