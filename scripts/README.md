# Run scripts

Run `bash validate.sh` for a complete offline check, `bash build.sh` to build and generate a seed, and `bash run_NGeniusFuzz.sh` to start an Open5GS-connected fuzzing session. The launcher automatically handles the crash-dump kernel setting and keeps the interactive UI disabled. All scripts resolve paths relative to the repository.
