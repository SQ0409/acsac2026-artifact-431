# Run scripts

Run `bash validate.sh` for a complete offline check, `bash build.sh` to build and generate a seed, and `bash run_aflnet.sh` to start a real Open5GS-connected fuzzing session. This also works when a Git checkout does not preserve executable bits. All scripts resolve paths relative to the repository.
